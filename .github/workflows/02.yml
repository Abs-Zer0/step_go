name: Practice Coding 02

on:
  push:
    branches:
      - master
    paths:
      - '02/signer/**'
  workflow_dispatch:

jobs:
  testing:
    name: Testing
    runs-on: ubuntu-24.04
    steps:
      - name: Prepare Repository
        uses: actions/checkout@v6
      - name: Prepare Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.21'
      - name: Run Tests
        run: |
          go test -v -race
        working-directory: '02/signer/'
